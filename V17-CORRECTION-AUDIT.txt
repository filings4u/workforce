screenings4u Compliance V17
Build 20260911-25

CORRECTION:
The prior patch could still load cached V15/V16 CSS/JS because the HTML asset URLs did not change when only the patch files were copied. V17 fixes that by using brand-new asset filenames.

Fresh shared assets:
- assets/css/customer-compliance-v17.css
- assets/js/navigation-v17.js
- assets/js/testing-orders-v17.js

Functional fixes:
- Testing Orders has no newBtn/modal/closeBtn/cancelBtn references.
- Testing Orders uses the dedicated testing-order-new.html workflow.
- All page HTML now points to the fresh V17 shared stylesheet/navigation filenames.
- Stat strips use auto-fit CSS grid so 4, 5, 6, or 8 metrics fill available width with no empty gray remainder.
- Page CTA buttons cannot wrap into two lines on desktop.
- Panel spacing and heading rhythm normalized.
- Workflow delay inputs use application styling.
- Security checkbox typography normalized.
- Active sidebar entry auto-scroll uses standards-compatible behavior.

Validation:
- 85 HTML routes checked.
- 91 JS files pass node --check.
- All internal HTML links resolve.
- All local asset references resolve.
