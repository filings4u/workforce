document.addEventListener("DOMContentLoaded",async()=>{try{const ctx=await CC.init();const q=await ctx.c.from("compliance_company_locations").select("*").eq("account_id",ctx.accountId).limit(100);if(q.error)throw q.error;const rows=q.data||[];body.innerHTML=rows.length?rows.map(x=>`<tr><td>${CC.esc(String(x.name ?? "—"))}</td><td>${CC.esc(String(x.location_type ?? "—"))}</td><td>${CC.esc(String(x.city ?? "—"))}</td><td>${CC.esc(String(x.phone ?? "—"))}</td><td>${CC.esc(String(x.status ?? "—"))}</td></tr>`).join(""):`<tr><td colspan="5" class="cc-empty">No records yet.</td></tr>`}catch(e){CC.fail(e)}});