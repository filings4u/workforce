let ctx,id;document.addEventListener("DOMContentLoaded",async()=>{try{ctx=await CC.init();id=new URLSearchParams(location.search).get("id");await head("documents");const q=await ctx.c.from("compliance_test_documents").select("document_type,visibility,original_name,mime_type,size_bytes,created_at,test_order_id").eq("account_id",ctx.accountId).eq("employee_id",id).order("created_at",{ascending:false});if(q.error)throw q.error;pageBody.innerHTML=table(["Date","Document","Type","Visibility","Size","Related Order"],(q.data||[]).map(x=>[CC.dt(x.created_at),x.original_name||"Document",x.document_type,CC.pill(x.visibility),x.size_bytes?`${Math.ceil(x.size_bytes/1024)} KB`:"—",x.test_order_id?`<a href="testing-order.html?id=${x.test_order_id}">Open</a>`:"—"]))}catch(e){CC.fail(e)}});async function head(a){recordNav.innerHTML=empNav(id,a);backLink.href=`employee-record.html?id=${id}`;const e=await ctx.c.from("employer_employees").select("first_name,last_name").eq("id",id).single();employeeName.textContent=`${e.data.first_name} ${e.data.last_name} — Documents`}function table(h,r){return `<div class="cc-table-wrap"><table class="cc-table"><thead><tr>${h.map(x=>`<th>${x}</th>`).join("")}</tr></thead><tbody>${r.map(a=>`<tr>${a.map(x=>`<td>${x??"—"}</td>`).join("")}</tr>`).join("")||`<tr><td colspan="${h.length}" class="cc-empty">No documents.</td></tr>`}</tbody></table></div>`}