document.addEventListener("DOMContentLoaded",async()=>{try{const x=await CC.init();if(x.mode!=="employee")return location.href="dashboard.html";const q=await x.c.from("compliance_test_documents").select("file_name,document_type,created_at").eq("visibility","employee").order("created_at",{ascending:false});if(q.error)throw q.error;body.innerHTML=(q.data||[]).map(v=>`<tr><td>${CC.esc(v.file_name||"Document")}</td><td>${CC.esc(v.document_type||"—")}</td><td>${CC.dt(v.created_at)}</td></tr>`).join("")||'<tr><td colspan="3" class="cc-empty">No employee documents.</td></tr>'}catch(e){CC.fail(e)}});