document.addEventListener("DOMContentLoaded",async()=>{try{const ctx=await CC.init();const q=await ctx.c.from("compliance_testing_programs").select("*").eq("account_id",ctx.accountId).order("name");if(q.error)throw q.error;body.innerHTML=(q.data||[]).map(x=>`<tr><td><strong>${CC.esc(x.name)}</strong></td><td>${CC.esc(x.program_type)}</td><td>${CC.esc(x.dot_agency||"—")}</td><td>${x.drug_testing_enabled?"Yes":"No"}</td><td>${x.alcohol_testing_enabled?"Yes":"No"}</td><td>${x.random_testing_enabled?"Yes":"No"}</td><td>${CC.esc(x.selection_frequency)}</td><td>${CC.pill(x.status)} <a class="cc-btn cc-btn-light" href="program-manage.html?id=${x.id}">Manage</a></td></tr>`).join("")||'<tr><td colspan="8" class="cc-empty">No programs yet.</td></tr>'}catch(e){CC.fail(e)}});