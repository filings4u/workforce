document.addEventListener("DOMContentLoaded",async()=>{try{const x=await CC.init();const q=await x.c.from("compliance_test_documents").select("*").eq("account_id",x.accountId).order("created_at",{ascending:false});if(q.error)throw q.error;body.innerHTML=(q.data||[]).map(v=>`<tr><td><strong>${CC.esc(v.original_name)}</strong></td><td>${CC.pill(v.document_type)}</td><td>${CC.pill(v.visibility)}</td><td>${CC.dt(v.created_at)}</td></tr>`).join("")||'<tr><td colspan="4" class="cc-empty">No documents have been released yet.</td></tr>'}catch(e){CC.fail(e)}});