let ctx,rows=[];document.addEventListener("DOMContentLoaded",async()=>{try{ctx=await CC.init();if(!ctx.isAdmin)return location.href="dashboard.html";rows=ctx.accounts||[];search.oninput=mode.onchange=status.onchange=render;body.onclick=e=>{let b=e.target.closest("[data-open]");if(b)CC.switchAccount(b.dataset.open,"dashboard.html")};render()}catch(e){CC.fail(e)}});function render(){let q=search.value.toLowerCase(),m=mode.value,s=status.value,a=rows.filter(x=>(!q||x.employer_name.toLowerCase().includes(q))&&(!m||x.operating_mode===m)&&(!s||x.account_status===s));body.innerHTML=`<table class="cc-table"><thead><tr><th>Employer</th><th>Mode</th><th>Plan</th><th>Subscription</th><th>Employees</th><th>Users</th><th>Open Tests</th><th>Status</th><th></th></tr></thead><tbody>${a.map(x=>`<tr><td><strong>${CC.esc(x.employer_name)}</strong><small>${x.portal_enabled?"Portal enabled":"Portal disabled"}</small></td><td>${CC.esc(x.operating_mode.replaceAll("_"," "))}</td><td>${CC.esc(x.plan_code||"—")}</td><td>${CC.pill(x.subscription_status)}</td><td>${x.employee_count}</td><td>${x.user_count}</td><td>${x.open_orders}</td><td>${CC.pill(x.account_status)}</td><td><div class="cc-row-actions"><a href="admin-employer.html?id=${x.account_id}">Manage</a><button class="cc-link-btn" data-open="${x.account_id}">Open Workspace</button></div></td></tr>`).join("")||'<tr><td colspan="9" class="cc-empty">No employers match these filters.</td></tr>'}</tbody></table>`}