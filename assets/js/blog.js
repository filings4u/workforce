(()=>{'use strict';
const API='https://rgsrubdtljyxmnihwlah.supabase.co/functions/v1/public-business-blog';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const date=v=>v?new Intl.DateTimeFormat('en-US',{month:'long',day:'numeric',year:'numeric'}).format(new Date(v)):'';
async function get(body){const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),d=await r.json().catch(()=>({}));if(!r.ok||d.error)throw new Error(d.error||'Unable to load Workforce articles.');return d}
async function list(){const root=document.getElementById('blogGrid');if(!root)return;try{const d=await get({site_code:'workforce',limit:40});const posts=d.posts||[];root.innerHTML=posts.length?posts.map(p=>`<article class="blog-card"><a class="blog-image" href="blog-post.html?slug=${encodeURIComponent(p.slug)}">${p.featured_image_url?`<img src="${esc(p.featured_image_url)}" alt="" loading="lazy">`:''}</a><div class="blog-card-body"><span class="blog-meta">${esc(p.category||'Workforce')} · ${esc(date(p.published_at))}</span><h2><a href="blog-post.html?slug=${encodeURIComponent(p.slug)}">${esc(p.title)}</a></h2><p>${esc(p.excerpt||'')}</p><a class="text-link" href="blog-post.html?slug=${encodeURIComponent(p.slug)}">Read article →</a></div></article>`).join(''):'<div class="empty-state">No Workforce articles are published yet.</div>'}catch(e){root.innerHTML=`<div class="empty-state">${esc(e.message)}</div>`}}
async function detail(){const root=document.getElementById('blogPost');if(!root)return;const slug=new URLSearchParams(location.search).get('slug');if(!slug){location.replace('blog.html');return}try{const d=await get({site_code:'workforce',slug}),p=d.post;document.title=(p.seo_title||p.title)+' | screenings4u Workforce';root.innerHTML=`<article class="article-shell"><header class="article-header"><span class="eyebrow">${esc(p.category||'Workforce')}</span><h1>${esc(p.title)}</h1><p class="article-lead">${esc(p.excerpt||'')}</p><div class="blog-meta">${esc(p.author_name||'screenings4u Workforce Team')} · ${esc(date(p.published_at))}</div></header>${p.featured_image_url?`<img class="article-hero-image" src="${esc(p.featured_image_url)}" alt="" loading="eager">`:''}<div class="article-content">${p.content_html||''}</div><div class="article-back"><a class="btn btn-outline" href="blog.html">← Back to Workforce Blog</a></div></article>`}catch(e){root.innerHTML=`<div class="empty-state">${esc(e.message)}</div>`}}
document.addEventListener('DOMContentLoaded',()=>{list();detail()});})();
