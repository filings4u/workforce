document.addEventListener("DOMContentLoaded",async()=>{try{const x=await CC.init();if(x.mode!=="employee")return location.href="dashboard.html";const q=await x.c.from("compliance_employee_program_enrollments").select("*,compliance_testing_programs(name)").eq("employee_id",x.employeeId);if(q.error)throw q.error;body.innerHTML=(q.data||[]).map(v=>`<tr><td>${CC.esc(v.compliance_testing_programs?.name||"Program")}</td><td>${CC.esc(v.effective_from||"—")}</td><td>${v.drug_eligible?"Yes":"No"}</td><td>${v.alcohol_eligible?"Yes":"No"}</td><td>${CC.pill(v.status)}</td></tr>`).join("")||'<tr><td colspan="5" class="cc-empty">No program enrollments.</td></tr>'}catch(e){CC.fail(e)}});