let rows=[];document.addEventListener("DOMContentLoaded",async()=>{try{let c=await CC.init(),q=await c.c.from("compliance_certificates").select("*").eq("account_id",c.accountId).order("expiration_date");if(q.error)throw q.error;rows=q.data||[];body.innerHTML=`<table class="cc-table"><thead><tr><th>Number</th><th>Type</th><th>Effective</th><th>Expires</th><th>Status</th><th>Verification</th></tr></thead><tbody>${rows.map(x=>`<tr><td><strong>${CC.esc(x.certificate_number)}</strong></td><td>${CC.esc(x.certificate_type)}</td><td>${x.effective_date}</td><td>${x.expiration_date||"—"}</td><td>${CC.pill(x.status)}</td><td>${CC.esc(x.verification_code||"—")}</td></tr>`).join("")}</tbody></table>`;exportBtn.onclick=()=>csv("certificate-register.csv",["Certificate Number,Type,Effective,Expiration,Status,Verification",...rows.map(x=>[x.certificate_number,x.certificate_type,x.effective_date,x.expiration_date||"",x.status,x.verification_code||""].map(v=>`"${String(v??"").replaceAll('"','""')}"`).join(","))].join("\n"))}catch(e){CC.fail(e)}});function csv(n,t){let a=document.createElement("a");a.href=URL.createObjectURL(new Blob([t],{type:"text/csv"}));a.download=n;a.click()}