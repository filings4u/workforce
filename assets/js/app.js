import {supabase} from './supabase.js';
export async function context(){const {data:{session}}=await supabase.auth.getSession();if(!session){location.href='login.html';return null}const {data,error}=await supabase.functions.invoke('workforce-session-context');if(error){console.error(error);return null}if(!data.has_access){location.href='access-required.html';return null}return data}
export function render(ctx){document.querySelectorAll('[data-org]').forEach(x=>x.textContent=ctx.membership?.organization_name||'Workforce');document.querySelectorAll('[data-plan]').forEach(x=>x.textContent=ctx.subscription?.plan?.name||(ctx.membership?.role_name||'Administration'));document.querySelectorAll('[data-role]').forEach(x=>x.textContent=ctx.membership?.role_name||'User');document.querySelectorAll('[data-entitlement]').forEach(x=>{const k=x.dataset.entitlement;if(ctx.entitlements?.[k]!==true)x.hidden=true});}
export async function logout(){await supabase.auth.signOut();location.href='login.html'}
