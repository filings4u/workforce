let rows=[];document.addEventListener("DOMContentLoaded",async()=>{try{let c=await CC.init(),q=await c.c.from("compliance_platform_audit_log").select("*").eq("account_id",c.accountId).order("created_at",{ascending:false}).limit(500);if(q.error)throw q.error;rows=q.data||[];render();search.oninput=render}catch(e){CC.fail(e)}});function render(){let q=search.value.toLowerCase(),a=rows.filter(x=>[x.action,x.entity_type,x.entity_id,JSON.stringify(x.details||{})].join(" ").toLowerCase().includes(q));body.innerHTML=`<table class="cc-table"><thead><tr><th>Date</th><th>Action</th><th>Record Type</th><th>Record ID</th><th>Details</th></tr></thead><tbody>${a.map(x=>`<tr><td>${CC.dt(x.created_at)}</td><td><strong>${CC.esc(x.action.replaceAll("_"," "))}</strong></td><td>${CC.esc(x.entity_type||"—")}</td><td><small>${CC.esc(x.entity_id||"—")}</small></td><td>${CC.esc(JSON.stringify(x.details||{}))}</td></tr>`).join("")||'<tr><td colspan="5" class="cc-empty">No audit records.</td></tr>'}</tbody></table>`}