document.addEventListener("DOMContentLoaded",async()=>{try{let c=await CC.init(),q=await c.c.from("compliance_communication_templates").select("*").or(`account_id.is.null,account_id.eq.${c.accountId}`).order("is_system",{ascending:false}).order("name");if(q.error)throw q.error;body.innerHTML=`<table class="cc-table"><thead><tr><th>Template</th><th>Key</th><th>Channel</th><th>Scope</th><th>Status</th><th></th></tr></thead><tbody>${(q.data||[]).map(x=>`<tr><td><strong>${CC.esc(x.name)}</strong><small>${CC.esc(x.subject||"")}</small></td><td>${CC.esc(x.template_key)}</td><td>${CC.esc(x.channel)}</td><td>${x.is_system?"System":"Account"}</td><td>${CC.pill(x.status)}</td><td>${!x.is_system?`<a href="communication-template-manage.html?id=${x.id}">Edit</a>`:""}</td></tr>`).join("")}</tbody></table>`}catch(e){CC.fail(e)}});