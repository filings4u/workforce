document.addEventListener("DOMContentLoaded",async()=>{try{let c=await CC.init(),q=await c.c.from("compliance_integrations").select("*").eq("account_id",c.accountId).order("integration_type");if(q.error)throw q.error;let a=q.data||[],n=s=>a.filter(x=>x.status===s).length;stats.innerHTML=[["Configured",a.length],["Active",n("active")],["Needs Attention",n("error")],["Inactive",n("inactive")+n("disabled")]].map(([l,v])=>`<div><strong>${v}</strong><span>${l}</span></div>`).join("");body.innerHTML=`<table class="cc-table"><thead><tr><th>Integration</th><th>Type</th><th>Provider</th><th>Status</th><th>Last Health Check</th><th></th></tr></thead><tbody>${a.map(x=>`<tr><td><strong>${CC.esc(x.display_name)}</strong><small>${CC.esc(x.last_error||"")}</small></td><td>${CC.esc(x.integration_type.replaceAll("_"," "))}</td><td>${CC.esc(x.provider_name)}</td><td>${CC.pill(x.status)}</td><td>${CC.dt(x.last_health_at)}</td><td><a href="integration-manage.html?id=${x.id}">Manage</a></td></tr>`).join("")||'<tr><td colspan="6" class="cc-empty">No integrations configured yet.</td></tr>'}</tbody></table>`}catch(e){CC.fail(e)}});