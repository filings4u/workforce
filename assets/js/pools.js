document.addEventListener("DOMContentLoaded",async()=>{try{const ctx=await CC.init();const q=await ctx.c.from("compliance_random_pools").select("*,compliance_testing_programs(name)").eq("account_id",ctx.accountId).order("name");if(q.error)throw q.error;body.innerHTML=(q.data||[]).map(x=>`<tr><td><strong>${CC.esc(x.name)}</strong></td><td>${CC.esc(x.compliance_testing_programs?.name||"—")}</td><td>${CC.esc(x.pool_type)}</td><td>${x.program_year}</td><td>${x.drug_rate??"—"}</td><td>${x.alcohol_rate??"—"}</td><td>${CC.esc(x.selection_frequency)}</td><td>${CC.pill(x.status)} <a class="cc-btn cc-btn-light" href="pool-manage.html?id=${x.id}">Manage</a></td></tr>`).join("")||'<tr><td colspan="8" class="cc-empty">No pools yet.</td></tr>'}catch(e){CC.fail(e)}});