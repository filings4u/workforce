let rows=[];document.addEventListener("DOMContentLoaded",async()=>{try{let c=await CC.init(),q=await c.c.from("compliance_random_selections").select("*,compliance_random_pools(name)").eq("account_id",c.accountId).order("selected_at",{ascending:false});if(q.error)throw q.error;rows=q.data||[];body.innerHTML=`<table class="cc-table"><thead><tr><th>Date</th><th>Pool</th><th>Period</th><th>Eligible</th><th>Drug</th><th>Alcohol</th><th>Status</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${CC.dt(x.selected_at)}</td><td>${CC.esc(x.compliance_random_pools?.name||"—")}</td><td>${CC.esc(x.selection_period)}</td><td>${x.eligible_employee_count}</td><td>${x.drug_selected_count}</td><td>${x.alcohol_selected_count}</td><td>${CC.pill(x.status)}</td></tr>`).join("")}</tbody></table>`;exportBtn.onclick=()=>csv("random-selection-report.csv",["Date,Pool,Period,Year,Eligible,Drug Selected,Alcohol Selected,Status",...rows.map(x=>[x.selected_at,x.compliance_random_pools?.name||"",x.selection_period,x.program_year,x.eligible_employee_count,x.drug_selected_count,x.alcohol_selected_count,x.status].map(v=>`"${String(v??"").replaceAll('"','""')}"`).join(","))].join("\n"))}catch(e){CC.fail(e)}});function csv(n,t){let a=document.createElement("a");a.href=URL.createObjectURL(new Blob([t],{type:"text/csv"}));a.download=n;a.click()}